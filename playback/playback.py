#First, prompt the user for an input

response = input("What playback speed do you want? ").replace(" ","...")

print(response)

#Then output the same input, replacing each space with ...(three periods)
#string.replace(oldvalue, newvalue, count)
